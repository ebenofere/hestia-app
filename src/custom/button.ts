import { styled } from "../../styled-system/jsx";

export const Button = styled("button", {
    base: {
        w: 'full',
        h: '56px',
        // rounded: '5px',
        color: '#282D32',
    }
});